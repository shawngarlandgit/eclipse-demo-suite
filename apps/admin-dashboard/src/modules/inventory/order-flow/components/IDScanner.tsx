/**
 * ID Scanner Component
 *
 * Re-exports from the refactored id-scanner module for backward compatibility.
 * The component has been modularized into smaller, focused files.
 *
 * @see ./id-scanner/ for the full module implementation
 */

export { IDScanner as default } from './id-scanner';
export type { IDScannerProps, ParsedLicenseData, ScannerStatus } from './id-scanner';
